{
  "name": "qiankun-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "applications:install": "npm-run-all --serial build install:*",
    "applications:start": "npm-run-all --parallel start:*",
    "install:main": "cd main && yarn",
    "start:main": "cd main && yarn start",
    "install:vue3": "cd vue3 && yarn",
    "start:vue3": "cd vue3 && yarn start"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
